{
  "name": "frontend",
  "version": "0.1.0",
  "scripts": {
    "serve": "vite",
    "build": "vite build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "vue": "^3.0.0",
    "simple.css": "latest"
  },
  "devDependencies": {
    "vite": "^4.0.0"
  }
}
